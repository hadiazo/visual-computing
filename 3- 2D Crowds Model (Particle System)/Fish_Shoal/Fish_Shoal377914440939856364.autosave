void setup(){
    size(800, 600);
    background(0);
    noStroke();
    frameRate(30);
}

void draw(){
    background(0);
    fill(255);
    ellipse(mouseX, mouseY, 20, 20);
}


class Fish{
    // Define properties of the fish such as Hunger, Libido, Fear, etc.
    PVector location;
}
